const Feature = require('app.feature');

class Log extends Feature {
  onCreate(done) {
    if (!this.api) {
      this.api = this.config.logger || console;
    }

    super.onCreate(done);
  }
}

module.exports = Log;
