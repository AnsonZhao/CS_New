require("./config/config.js");

var Error=require("../resolve_error_index.js").Error;


before(function (done) {
    MongoClient = require('mongodb').MongoClient;
    MongoClient.connect('mongodb://127.0.0.1:27017/dev_log', function (err, db) {
        if (err) throw err;
        DB = db;
        done()
    })
})

describe('#make error', function () {
    it('makes and logs', function (done) {
        var E=new Error({name:'one',source:'unit_test',function:'make',module:'test'},{rep:'1',one:'other'});
        console.log(E);
        done()
    });


})